class Validator {
  constructor(field, configuration) {
    this.field = field;
    this.configuration = configuration;
  }
  validate(obj) {
    if()
    return false;
  }
}

module.exports = {
  Validator
}